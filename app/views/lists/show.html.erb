<div class="d-flex align-items-center">
  <h1 class="text-light me-5 mb-0"><%= @list.name %></h1>
  <%= link_to "Add bookmark", new_list_bookmark_path(@list), class: "btn btn-primary" %>
</div>

<ul class="mt-5 ps-0">
  <div class="cards wide">
  <% @list.bookmarks.each do |bookmark| %>
    <li>
      <div class="card d-flex flex-column align-items-start justify-content-between text-light bg-dark gap-2">
        <img src=<%= bookmark.movie.poster_url %> alt="" width="100%" class="mb-3 align-self-center">
        <div class="d-flex justify-content-between w-100 fw-bold">
          <h2 class="h3"><%= bookmark.movie.title %></h2>
          <span><%= bookmark.movie.rating %><i class="fas fa-star text-info m-1"></i></span>
        </div>
          <p><%= bookmark.movie.overview %></p>
          <div class="bottom-info">
            <%= link_to "Delete", bookmark_path(bookmark), method: "delete", class: "btn btn-danger btn-lg text-light my-2" %>
            <blockquote>
              <q>
                <%= bookmark.comment %>
              </q>
            </blockquote>
          </div>
      </div>
    </li>
  <% end %>
  </div>
</ul>
